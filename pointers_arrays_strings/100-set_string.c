#include "main.h"
/**
 * set_string - affecte la valeur d'un pointeur vers une chaîne
 * @s: double pointeur vers le caractère
 * @to: pointeur vers la chaîne de caractères à affecter
 *
 * Return: Rien
 */
void set_string(char **s, char *to)
{
*s = to;
}

