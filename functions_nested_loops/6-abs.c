#include "main.h"
/**
 * main - checks the instructions
 * @void: type int
 *
 * Description:
(* section header: Section description)*
 * Return: Always return 0.
 */

int _abs(int n)
{
    if (n < 0)
    {
        return -n;
    }
    return n;
}
#endif
