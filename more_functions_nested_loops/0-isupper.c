#include <stdio.h>
#include "main.h"

 /**
  * main -  check the code
  * @void:
  * Description: checks the function
  * Return: 1 or 0.
  */
int _isupper(int c);

int main(void)
{
    char c;
    
    if (_isupper(c))
    {
        return (1);
    }
    else
    {
        return (0);
    }
  }
